# Gas City
.gc/
.beads/*
.codex/*
.claude/
!.beads/config.yaml
!.beads/metadata.json
hooks/
.runtime/

# Nested rig (has its own git history)
hello-world/

# Local agent tooling (skills installed locally, like .claude/)
.agents/

# Personal reference — never publish (account topology, emails, SSH layout)
MULTI_ACCOUNT_SETUP.md

# macOS
.DS_Store
