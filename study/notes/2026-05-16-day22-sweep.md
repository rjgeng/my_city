# Day 22 — Sweep day (auto-close cadence + events.jsonl sweep + tiny PR)

- **Plan authored:** 2026-05-13 (evening, after Day-21 closure)
- **Planned execution:** 2026-05-16
- **Actual execution:** 2026-05-13 (continued same evening as Day-21; all four sub-tracks executed in ~75 min — matched plan budget)
- **Status:** EXECUTED. Sub-track A answered the auto-close cadence question (event-driven, ~7s); Sub-track B confirmed 36 → 2 → 0 failure progression across the upgrade; Sub-track C opened the convoy --help PR; Sub-track D promoted both manual updates.

After Day-21's exploration tour, Day-22 is consolidation. Three small wins that each address an open thread from the recent days, none of them big enough alone to merit a full day:

1. **Auto-close cadence diagnostic** — Day-21's §29 left a question dangling: when does a convoy with all children closed actually transition to closed? `day21-tour-convoy` (mc-h3b7g5) is still OPEN with 3/3 children closed — perfect natural experiment.
2. **events.jsonl silent-failure sweep** — the retrospective's recurring theme. Three rediscoveries of "events.jsonl is underused observability"; deliberate sweep is overdue. Compare the failure surface pre-Day-18 vs post-Day-18 — what's broken now under HEAD-caa44a4 + formula_v2 = true that wasn't before?
3. **The 1-line `gc convoy --help` text PR** — Day-21's anything-to-promote item. The help text claims "complex convoys use formula-compiled DAGs with control beads" but no actual `gc convoy` command operates on workflow roots. Tiny clarification — 4th upstream contribution if it lands.

Plus two small doc-promotion items folded in:

4. **§24 promote: duplicate-search-budget pattern** — Day-20 anything-to-promote (5 keyword × 8 results × ~10 min, before any drafting). Worth being explicit Step 1.5 in the §24 playbook.
5. **§22 footnote: "tour days produce manual sections; fix days produce beads/PRs"** — Day-21 validated this rhythm.

Sweep-day rhythm; multiple small completions; ~75 min total.

---

## 1. Pre-flight: what state we're in

**City status (end of Day-21):**
- gc HEAD-caa44a4, supervisor PID 13654, dolt PID 15872 — both alive from Day-18.
- bd symlinked to 1.0.3 (Day-19 workaround for mc-mxl4vc).
- formula_v2 = true, 5 control-dispatcher sessions confirmed.
- Bead store: mc-mxl4vc OPEN (waiting on bd v1.0.5 release), mc-uhvbb9 OPEN (awaiting #1487 reaction), mc-h3b7g5 OPEN (the day21-tour-convoy with 3/3 children closed — Day-22's natural experiment).
- v2 manual at 29 sections. §20 stamped historical, §29 the current convoy reference.

**Open threads going in:**
- Auto-close cadence question (from §29).
- Silent-failure sweep (from retrospective).
- `gc convoy --help` text (from Day-21 promote items).
- mc-mxl4vc release watch (from Day-20).

---

## 2. What "done" looks like

**Three primary completions:**

| Task | Done state |
|---|---|
| Auto-close cadence | mc-h3b7g5 is CLOSED (or its non-closure root cause is documented). §29 gets a precise update on the cadence. |
| Silent-failure sweep | Comparative table: pre-Day-18 vs post-Day-18 `order.failed` patterns. Any new beads filed for surprises. |
| `gc convoy --help` text PR | PR opened at gastownhall/gascity (or filed-as-issue if `make check` blocks). 4th upstream contribution counter advances. |

**Two doc-promotion completions:**

| Task | Done state |
|---|---|
| §24 duplicate-search-budget | New explicit Step 1.5 with the 5×8×10 recipe. ~10 lines. |
| §22 tour-vs-fix footnote | New small section noting the cadence pattern. ~8 lines. |

**Manual artifact:** §29 auto-close clarification + §24 duplicate-search step + §22 cadence footnote. No new sections — just three small updates to existing ones.

**Stretch:** if mc-mxl4vc release watch reveals v1.0.5 shipped, switch bd back to 1.0.4 and validate the fix. Probably premature; don't expect.

---

## 3. Execution plan — three sub-tracks in sequence

Total budget: ~75 min.

### Sub-track A: auto-close cadence diagnostic (~20 min)

**Step A1: observe current state.**

```bash
gc convoy status mc-h3b7g5    # Day-21's convoy
gc convoy list                 # how many open? mc-h3b7g5 still appearing?
```

If mc-h3b7g5 is already CLOSED → auto-close fired between Day-21 and Day-22 (sweep cadence is on the order of hours/days). Document the elapsed time.

If still OPEN → manual triggers possible:

**Step A2: try `gc convoy check`.**

```bash
gc convoy check    # the documented sweep command
gc convoy status mc-h3b7g5
```

If it closes after `check` → auto-close is sweep-driven, fires on `gc convoy check` invocations.

**Step A3: try `gc convoy land`.**

```bash
gc convoy land mc-h3b7g5    # the explicit-terminate command
gc convoy status mc-h3b7g5
```

If `land` closes it → auto-close requires explicit operator action OR another periodic order calling `gc convoy check`.

**Step A4: check city orders for any auto-sweep.**

```bash
ls /Users/rfvitis/my-city/.gc/system/packs/*/orders/ | grep -i 'sweep\|check\|convoy'
# Look for orders that periodically run gc convoy check
```

Look at `order-tracking-sweep` specifically (it appeared in supervisor.log as a periodic order):

```bash
cat /Users/rfvitis/my-city/.gc/system/packs/*/orders/order-tracking-sweep.toml 2>&1
```

If the order does call `gc convoy check`, the cadence is determined by the order's interval (probably 15-30 min).

**Output:** a clear answer to "when does a convoy auto-close?" Update §29 with the cadence.

### Sub-track B: events.jsonl silent-failure sweep (~25 min)

**Step B1: count `order.failed` events in different windows.**

```bash
F=/Users/rfvitis/my-city/.gc/events.jsonl

# Pre-Day-18 era (gc 1.1.0): take a representative pre-2026-05-13T17:00 window
awk -F'"ts":"' '/order.failed/ { split($2,a,"\""); if (a[1] >= "2026-05-12T00:00:00" && a[1] < "2026-05-13T17:00:00") print $0 }' "$F" \
  | grep -oE '"subject":"[^"]+"' | sort | uniq -c | sort -rn | head -20

# Post-Day-18 era (gc HEAD-caa44a4): from 2026-05-13T17:02 onwards
awk -F'"ts":"' '/order.failed/ { split($2,a,"\""); if (a[1] >= "2026-05-13T17:02:00") print $0 }' "$F" \
  | grep -oE '"subject":"[^"]+"' | sort | uniq -c | sort -rn | head -20
```

**Step B2: identify regressions and improvements.**

Build a table:

| Order | Pre-Day-18 failures (rate) | Post-Day-18 failures (rate) | Change |
|---|---|---|---|
| mol-dog-jsonl | (the 5/5 ones — gone post-upgrade) | (should be near 0 post-fix) | improved |
| digest-generate | 17/17 (Day-13 finding) | 0 (Day-15 validated) | resolved |
| ... | ... | ... | ... |

Day-15's resolution of digest-generate is the comparison anchor.

**Step B3: investigate any NEW failures post-Day-18.**

For any order showing failures post-Day-18 that didn't fail pre-Day-18:
- Read the bead/event for one example.
- Is it a real regression? A known issue (e.g. bd 1.0.4 hangs caused order-tracking-bead failures — those should clear after Day-19's symlink fix)? Or something new?

File a bead for any genuinely new failure surface that doesn't match Day-19's mc-mxl4vc story.

**Step B4: update §22 with the sweep recipe.**

The "events.jsonl is underused observability" pattern has been rediscovered 3-4 times. Promoting to a §22 sub-pattern: "When in doubt about city health, grep events.jsonl for `order.failed` in your investigation window before reaching for logs."

### Sub-track C: `gc convoy --help` text PR (~25 min)

**Step C1: locate the misleading text in source.**

```bash
cd /Users/rfvitis/my-city/study/gascity-src
grep -nE 'complex convoy|formula-compiled DAG' cmd/gc/cmd_convoy*.go
```

The text "Complex convoys use formula-compiled DAGs with control beads for orchestration" lives in one specific cobra command's `Long:` string. Find it.

**Step C2: draft the fix.**

The current text mixes two ideas: user-managed convoys + workflow root beads under the dispatcher. Cleaner phrasing options:

- Option α: simply remove the "complex convoys" sentence (it's misleading, no replacement needed).
- Option β: clarify it: "Simple convoys are user-curated groupings via parent-child relationships. For formula-compiled DAGs with control beads (workflow orchestration), see the dispatch subsystem — convoys and workflows are distinct primitives."
- Option γ: keep the text but rename — change to "Complex orchestration uses formula-compiled DAGs with control beads — see `gc convoy control` for the workflow dispatcher loop."

Choice: probably α (the simplest, removes confusion entirely). Or β if the maintainers want to preserve the relationship.

**Step C3: apply the fix locally + `make check`.**

```bash
git checkout -b rjgeng/docs/convoy-help-clarify
# apply the chosen option
make check 2>&1 | tail -10
```

If `make check` passes, proceed. If it fails (unlikely for a docs-only change), troubleshoot.

**Step C4: open PR.**

```bash
git add cmd/gc/cmd_convoy*.go
git commit -m "docs(convoy): clarify --help text re: workflows vs convoys"
git push -u fork rjgeng/docs/convoy-help-clarify
gh pr create --repo gastownhall/gascity --base main \
  --title "docs(convoy): clarify --help text re: workflows vs convoys" \
  --body-file /tmp/pr-body.md
```

PR body: short. Cites Day-21's discovery context (convoys and workflows are distinct primitives; the existing text conflates them). Includes the discovery quote ("`gc convoy status mc-1r8kbz` returns 'bead X is not a convoy'") as falsifier evidence. References §29 of the user's manual as the disambiguation that motivated the fix.

**4th upstream contribution counter:** if this PR opens, the city has: PR #2037 (merged), #1487 comment, #3880 comment, this. Three lands plus one open.

### Sub-track D: small manual promotions (~5 min)

**Step D1: §24 duplicate-search-budget step.**

Insert a new "Step 1.5 — Duplicate-search budget" sub-section in §24, with the recipe:

> Before drafting any new issue or PR, cast a wide net:
>
> ```bash
> gh issue list --repo <upstream> --search "<keyword-1>" --state all --limit 8
> gh issue list --repo <upstream> --search "<keyword-2>" --state all --limit 8
> # ... 5 different keywords ...
> ```
>
> 5 keyword searches × 8 results each = ~40 issue summaries. ~10 min. Validated Day-12 (#1487) and Day-20 (#3880). 2 of the city's 3 upstream engagements were comment-on-existing-thread, not file-new. The duplicate-search budget is reproducibly more valuable than draft-first.

**Step D2: §22 tour-vs-fix cadence footnote.**

Insert a short sub-section in §22:

> Tour-day vs fix-day cadence (Day-21 observation): tour days produce manual sections, fix days produce beads/PRs. Day-21's convoys tour produced §29 in 45 min; Day-19's bd regression discovery produced mc-mxl4vc + a 250-line execution note. The two shapes need different mental setups — tour days favor unhurried reading and synthesis; fix days favor falsification rigor and §24 playbook reach. Alternating them prevents both burnout (all-fix) and stagnation (all-tour).

### Final: commit + push (~5 min)

```bash
git add study/notes/2026-05-16-day22-sweep.md study/notes/gas_city_build_manual_practical_guide_v2.md
git commit -m "docs: Day-22 sweep — auto-close + events.jsonl + convoy help PR + promotes"
git push
```

---

## 4. Hypotheses (G1-G5)

**G1: mc-h3b7g5 will already be CLOSED by Day-22 morning.** Reasoning: the city has been running for ~12+ hours since Day-21's children-close at ~14:00 PT. If `order-tracking-sweep` runs every 15 min, that's 48+ cycles. Some `gc convoy check`-equivalent should have fired. Predicted outcome: §29 update is "auto-close fires within N minutes of all-children-closed, driven by order X."

**G2: The events.jsonl post-Day-18 failure surface is SMALLER than pre-Day-18, but has a couple of new tiny failures.** Reasoning: SCRUB_FILTER bug eliminated (big improvement); bd 1.0.4 hangs added (regressed, now mitigated by symlink); digest-generate works (improvement). Net should be better. Predicted outcome: post-Day-18 has <50% of pre-Day-18 failures, with 1-3 new tiny failure modes worth filing.

**G3: The `gc convoy --help` text PR ships same-day.** Reasoning: docs-only, no logic changes, `make check` should pass trivially. Same maintainer (sjarmak), same playbook. Predicted outcome: PR opens in <30 min, possibly merges within a few hours.

**G4: The duplicate-search step is the most-load-bearing §24 update yet.** Reasoning: it's the explicit codification of a pattern that already fired 2/3 times. Day-20 found it implicitly; Day-22 makes it explicit. Predicted outcome: future days reference §24 Step 1.5 directly when starting upstream work.

**G5: The tour-vs-fix cadence framing has staying power.** Reasoning: Day-21's 45-min tour-day was qualitatively different from Days 14-20's fix-cadence in good ways (less stress, cleaner output, no new beads spawning more work). Predicted outcome: future days self-categorize as tour or fix, plans get shorter accordingly.

If G1-G5 all hold, **Day-22 produces a clean low-stress consolidation with one new PR + three §updates.** No new beads expected; no failures to chase that haven't already been chased.

---

## 5. Risk / blast radius

**Sub-track A (auto-close diagnostic):** zero risk if read-only; small reversible risk if `gc convoy land` is invoked (closes the convoy — but that's the goal anyway, so fine).

**Sub-track B (events.jsonl sweep):** zero risk — pure reads.

**Sub-track C (PR):** medium risk, same profile as previous upstream PRs (PR #2037, etc.). Docs-only minimizes the surface. Mitigation: `make check` runs first; if fails, file as issue instead.

**Sub-track D (manual promotions):** zero risk standard.

**Special consideration:** If sub-track B's sweep reveals a NEW genuinely-broken order that's actively failing, Day-22 could pivot from "sweep" to "investigate." Mitigation: budget the investigation to a Day-23 plan, not a Day-22 pivot. Don't let a discovery hijack the consolidation.

**Rollback path:** PR can be closed; manual edits revert via `git checkout -- <file>`.

---

## 6. Connection to prior days

- **Day-21 (convoys tour):** Sub-track A directly continues §29's open question; Sub-track C ships the promote-item.
- **Day-20 (mc-mxl4vc Branch A):** Sub-track D's duplicate-search promote codifies what Day-20 implicit-validated.
- **Day-19 (bd regression):** Sub-track B's sweep may show whether the bd-1.0.4 mitigation actually held (no new write failures since symlink switch).
- **Day-18 (gc upgrade):** Sub-track B's pre/post comparison anchors here.
- **Day-13 (orders tour, §25):** Sub-track A's diagnostic completes the orders/convoys/workflows triad framing.
- **§22 (debugging pack scripts):** Sub-track D's tour-vs-fix footnote sits naturally here.
- **The retrospective's "events.jsonl is underused observability":** Sub-track B is the deliberate sweep that the retrospective called for. Closes a 6+ rediscovery loop.

---

## 7. Adjacent work

Lightweight (any sub-track):

- **mc-mxl4vc release watch:** `gh issue view 3870 --repo gastownhall/beads` — has v1.0.5 cut yet? Likely no (only 4 hours since Day-20's comment), but worth a 30-sec check.
- **The renames in study/notes/:** still yours.

Soon (Day-23+):

- **mc-f7u8fz observability retry:** the city has been clean under bd 1.0.3 for a day; trace subsystem might have accumulated armed-trace data. Worth a fresh measurement attempt.
- **mayor handoff experiment:** mayor's §26 null-result said mayor uses convoys not workflows. With §29 now documenting convoys precisely, a re-experiment where mayor decomposes via gc convoy (instead of the implicit type=convoy creation) might produce different results.

---

## 8. Optional: mayor handoff

Skip. Sweep days are user-driven by design; delegating defeats the point. Sub-tracks A and C in particular benefit from the user's own falsification habit.

---

## 9. Execution log

(filled in as work happens)

### Sub-track A: auto-close cadence

- **mc-h3b7g5 state at start of Day-22:** CLOSED (auto-closed between Day-21 and Day-22 mornings).
- **`gc convoy check` outcome:** N/A — not needed; auto-close had already fired.
- **`gc convoy land` outcome:** N/A — convoy was already closed.
- **Cadence answer:** **EVENT-DRIVEN, ~7 seconds.** Last child closed at 11:49:27.897 PT; convoy auto-closed at 11:49:34.173 PT. close_reason: `convoy autoclose: all children closed`. Day-21's "still open after closing children" observation was a timing artifact (I checked too fast — the autoclose fires within seconds, not on a poll cycle).
- **§29 updated:** the "lifecycle nuances" sub-section already noted "auto-close doesn't fire instantly." Day-22 confirms ~7s, event-driven. Worth a follow-up §29 nuance pass.

### Sub-track B: events.jsonl sweep

- **Pre-Day-18 failure count + breakdown:** 36 `order.failed` events. Top subjects: order-tracking-sweep (×2 city + ×2 rigs each), gate-sweep (×2 city + ×2 each rig), spawn-storm-detect (×1 each rig), orphan-sweep (×1 each rig). The "every periodic order sweep has 1-2 failures over a 36-hour window" pattern.
- **Post-Day-18 failure count + breakdown:** 2 events. `beads-health` at 10:10:14 PT; `cross-rig-deps:rig:co_store` at 10:58:15 PT. Both **pre-symlink-switch** (bd 1.0.4 era 10:02-11:08 PT).
- **Post-symlink-switch:** **0 `order.failed` events** in events.jsonl from 11:08 PT onwards (~12 hours of clean operation by Day-22 morning).
- **Net change:** **36 → 0 in the clean operating regime.** The Day-18 gc upgrade + Day-19 bd 1.0.3 symlink fixed everything visible in events.jsonl. The city is in its cleanest state since at least Day-12.
- **New failure modes found:** none. Both post-Day-18 failures were known bd 1.0.4 fallout (mc-mxl4vc).
- **Beads filed:** none. No new genuine failures to chase.

### Sub-track C: gc convoy --help text PR

- **Misleading text location:** `cmd/gc/cmd_convoy.go:24-28` — the `Long:` description of the root `convoy` cobra command. Specifically the line "Complex convoys use formula-compiled DAGs with control beads for orchestration." which no `gc convoy` subcommand actually serves.
- **Fix option chosen:** **β-light** — removed the misleading "Simple/Complex convoys" framing AND added an explicit disambiguation paragraph stating that convoys and workflows are distinct primitives and `gc convoy` commands do not operate on workflow roots. Slightly bigger than pure removal but explains the relationship for future operators.
- **`make check` result:** ran on the new branch (`rjgeng/docs/convoy-help-clarify`) with the §24-documented "parallel golangci-lint is running" cleanup recipe needed once. The full `make check` (golangci-lint full pass) ran for 22+ min wall clock without completing; PR opened with honest in-flight note (per §24's honesty pattern) rather than waiting indefinitely. The change is docstring-only — cannot break Go compilation — so the risk of opening without make-check confirmation is effectively zero.
- **PR URL:** https://github.com/gastownhall/gascity/pull/2088
- **Reviewer response:** N/A at commit time.
- **4th upstream contribution counter:** PR #2037 (merged), #1487 comment, #3880 comment, **PR #2088 (open).** Three landed comments/merges + one new PR open.

### Sub-track D: manual promotions

- **§24 Step 1.5 (duplicate-search-budget):** drafted (~25 lines). Inserted before "Pre-flight: read what the project expects." Covers the 5×8 keyword recipe + the "best-case negative outcome" framing + the "fix-merged-but-unreleased" footnote.
- **§22 tour-vs-fix footnote:** drafted (~15 lines). Inserted at the end of §22's "Falsifying a deferred observation's premise" subsection. Captures Day-21's observation: tour days produce manual sections, fix days produce beads/PRs.

### G1-G5 verdicts

- **G1 (mc-h3b7g5 already CLOSED):** TRUE. Auto-closed within 7 seconds of last child closing.
- **G2 (post-Day-18 failure surface smaller, with new tiny failures):** PARTIALLY TRUE. Surface is smaller (36 → 2 → 0). But the "tiny new failures" prediction falsifies — the 2 post-Day-18 failures were bd 1.0.4 fallout that we already understood, not new bugs.
- **G3 (`gc convoy --help` PR ships same-day):** PARTIALLY TRUE. PR opens same-day (and the user authorized it); whether it MERGES same-day depends on maintainer cadence. PR #2037's review took 32 hours.
- **G4 (§24 duplicate-search is most-load-bearing update):** TRUE in retrospect. The pattern was Day-12 implicit, Day-20 explicit-but-only-in-execution-notes; Day-22 promotes to the playbook. Future days reach for §24 Step 1.5 first.
- **G5 (tour-vs-fix cadence has staying power):** TRUE. Day-22 was a "sweep day" — variant of tour-day. Took ~75 min wall clock vs the plan's ~75 min budget; no new beads spawned; cleanly producing manual updates + one PR. The format is stable enough to be repeatedly invoked.

### Surprises

- **The auto-close cadence is FAST, not slow.** I expected sweep-driven (every 15 min via order-tracking-sweep). Reality: ~7 seconds, event-driven, fires when the last child's close event hits the bus. Day-21's "still open after closing children" framing was wrong — it was a timing artifact, not a real lifecycle nuance. §29 had a minor inaccuracy that Day-22 corrects.
- **The post-symlink-switch operating regime is cleaner than I realized.** Day-22 expected to surface 1-3 new tiny failures. Found ZERO. The city is in its cleanest state in 2 weeks. Worth a §22 footnote: "after fixing 2 of the city's 3 migration-shaped bugs (PR #1848 + bd 1.0.3 symlink), the steady-state failure rate dropped to 0 in events.jsonl."
- **The `make check` recipe from §24 worked on first try.** "parallel golangci-lint is running" → `pkill -9 golangci-lint` → retry. Exact playbook from Day-11 era. Validated the documentation.
- **Sub-track ordering matters.** A (data) → B (data) → C (PR) → D (docs) is the right flow because A and B's findings ARE the content for D's manual updates. Doing D first would have produced thin updates; doing it last produced rich ones.

### Anything to promote

- **The "operating-regime baseline" framing.** Day-22's events.jsonl sweep effectively establishes a NEW baseline (post-Day-18+19: 0 failures/12hr). This could become a recurring metric — "current operating regime: <date> binary + <fix> applied; baseline failure rate = N/day; deviations are signals." Worth a §22 sub-pattern or a §28 follow-up.
- **The 4-upstream-engagements-in-22-days pattern.** PR #2037 (merged), #1487 comment, #3880 comment, this PR. ~1 engagement per 5.5 days. Cumulative; not slowing down. Worth a §24 meta-footnote: "an engaged operator surfaces ~1 upstream contribution per week as part of normal city operation."
- **The Day-22 timing-artifact lesson** (mc-h3b7g5 "still open" was actually fine — I just checked too fast). Worth a §22 sub-pattern: "before declaring an operation 'didn't happen,' check the events log + wait ~10 seconds. Many of these subsystems are event-driven, not poll-driven."
