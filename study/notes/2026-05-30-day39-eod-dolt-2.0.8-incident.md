# Day 39 EOD — dolt 2.0.8 wisp corruption (soak day became an incident day)

- **Day:** 39 (planned as the mc-jhsp8y re-soak read-day; executed 2026-05-30)
- **Plan reference:** soak-watch setup + `study/notes/templates/soak-evidence-template.md` (built this morning); incident record in `study/notes/adr/0003-dolt-2.0.8-wisp-corruption-recovery.md`
- **Actual execution:** the ~10:48 PT soak fire never dispatched → the supervisor was down because the dolt data plane was panicking → full incident diagnosis, recovery attempt, upstream filing, blast-radius, and wrap.

## What happened
1. Built `study/scripts/soak-watch.sh` + the soak-evidence template (per-fire G5 capture) for the mc-jhsp8y re-soak.
2. Fire never came. `gc status` panicked inside dolt: `search wisps (merge): invalid hash length: 19` (`AdaptiveValue.convertToTextStorage` → `hash.New`). The all-night "supervisor not running" was a **data-plane artifact**, not a real wedge.
3. **Root cause:** the Day-38 `gc upgrade` (HEAD-fad5d3f→8d6d6bb) pulled bundled **dolt 2.0.4→2.0.8**; the wisp **nonlocal-table migration** under 2.0.8 corrupted `hq.wisps` out-of-line adaptive longtext. Write-side corruption — both 2.0.8 and 2.0.4 reject the bytes.
4. **Recovery:** downgraded dolt→2.0.4 (manual symlink). Attempted drop + `bd migrate schema` → dead-ended on the **nonlocal-federation ledger wall** (0007 expects wisps; tables are runtime-materialized, not committed dolt objects). Restored the 5.7G backup → back to the known state. **my-city controller parked.**
5. **Upstream:** filed **dolt#11131** (root cause) + **gascity#2814** (consumer; pin 2.0.4). Posted control evidence + a drift diagnosis + a PR offer on #2814 (§24 HOLD — julianknutsen self-assigned the P0).
6. **Blast-radius:** only my-city corrupted. gastownhall-logs (was 2.0.8, inline-only → intact) restarted onto 2.0.4; my-llm-wiki already 2.0.4; 4g-city dolt down → starts on 2.0.4. **No city left on 2.0.8.**

## Verdicts
- **mc-jhsp8y soak — PAUSED, not failed.** Cannot fire until the wisp data plane is rebuilt. G5 never reached (no valid fire).
- **The Day-38 upgrade backfired:** it was done to enable the soak (PR #2564/#2598 gate) but shipped a worse regression (data-loss) than it fixed.
- **gascity#2814 → P0** by julianknutsen ~32 min after filing, self-assigned — strong validation the report landed.

## State at close (~14:10 PT)
- All four cities on dolt **2.0.4**; my-city controller **down** (by design, awaiting recovery); **dolt symlink 2.0.4 is a FRAGILE manual override** — do NOT `brew link`/`brew upgrade`/`gc upgrade`.
- Backups: `.beads/dolt.backup-20260530T1129-pre-reset` (5.7G), `.beads/dolt.BROKEN-postsurgery-20260530` (forensics).
- Commits: `d382f9c` (incident + tooling + upstream drafts) + `50e958e` (P0 triage, blast-radius, §24 hold).
- Memory: `project_dolt208_wisp_corruption_soak_paused`.
- Day-41 plan: `study/notes/2026-05-31-day41-plan-dolt-incident-followup.md` (the 05-30 incident spanned the Day-39–40 numbering; the next work-session is Day-41).

## Surprises / lessons
- **Floor ≠ pin.** `doltversion.go ManagedMin="2.0.7"` is a minimum, so the bad 2.0.8 (≥ floor) floated straight through the "pin." Engine boundary: 2.0.7=`20260526` (safe) → 2.0.8=`20260528` (bad).
- **Engine downgrade can't un-corrupt** already-written bytes; upstream fixes prevent recurrence but do NOT recover my-city. my-city recovery is a local, independent track.
- **The bug blocks its own bug report** — bd writes fail on the wisp migration, so the tracking bead can't be filed.
- **Liveness-signal lesson re-confirmed** ([[liveness_signals_unreliable_during_dolt_degradation]]): supervisor-down was an artifact; fixed the data plane first.
- dolt turns this subsystem around fast (#11095 same-day) — engine fix plausibly days.

## Bead state — NO mutations possible this day
bd **writes are blocked** by the corruption (`0006_add_wisp_is_blocked` fails on DB-open), so unlike a normal EOD there is **no `bd note` appended**:
- **mc-jhsp8y** — intended note "soak PAUSED by dolt 2.0.8 wisp corruption; see ADR-0003" could NOT be written; deferred.
- **upstream-tracking bead** — DEFERRED (would-be title recorded in ADR-0003).
- All bead updates wait on the my-city data-plane rebuild.

## Carry-forward → Day-41
- Watch #2814 (P0) + #11131; verify his fix or send the prepared `doltversion.go` known-bad-block PR per §24 (STOP-before-`pr create`).
- my-city recovery scouting (G3) — beads-native wisp rebuild on a good engine (2.0.7 now confirmed pre-regression); investigate-only, no destructive retry without auth + backup.
- Once bd writes recover: append the deferred mc-jhsp8y note + file the tracking bead.
